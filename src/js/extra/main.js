import * as PIXI from 'pixi.js';
window.PIXI = PIXI;

import { gsap } from 'gsap';
window.gsap = gsap;

import { Howler, Howl } from 'howler';

window.UrsoUtils = {
    Howler,
    Howl,
    gsap,
    PIXI
};


export default {}
